# OS
.DS_Store

# Dependencies
node_modules/

# Local scratch (screenshots, throwaway artifacts)
.tmp/

# Codex CLI local config (per-machine, not part of the project)
.codex/

# External reference projects (cloned separately — see REQUIREMENTS.md §8)
reference/

# Vercel
.vercel

# Env files (defense in depth; web/.gitignore also covers this)
.env
.env.*
!.env.*.example
